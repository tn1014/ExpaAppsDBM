-- 更新日時をセットする関数
CREATE OR REPLACE FUNCTION SET_UPDATED_AT() RETURNS TRIGGER AS $BODY$
BEGIN
    NEW.updated_at = NOW();
    RETURN NEW;
END;
$BODY$
LANGUAGE plpgsql;

ALTER FUNCTION SET_UPDATED_AT OWNER TO expa_apps;