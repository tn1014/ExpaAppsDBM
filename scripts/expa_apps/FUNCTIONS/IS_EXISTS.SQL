-- 数値をBOOLEAN型に変換する
CREATE FUNCTION IS_EXISTS(BIGINT) RETURNS BOOLEAN AS $BODY$
BEGIN
	IF $1 IS NOT NULL AND $1 > 0 THEN
		RETURN TRUE;
	ELSE
 		RETURN FALSE;
 	END IF;
END;
$BODY$
LANGUAGE plpgsql;

ALTER FUNCTION IS_EXISTS OWNER TO expa_apps;